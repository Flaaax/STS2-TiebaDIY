# Bug 记录

这些BUG要么难以解决，要么来自原版游戏。此模组没有擅自修复它们，但已经尽量避免它们。

## “电子羊”多人游戏不同步

### 现象

在多人游戏中打出“电子羊”后，生成候选牌时可能立刻出现校验和不一致。在本次捕获的日志中，各端的差异仅存在于共享的 `CombatCardGeneration` 随机数生成器：其中一个客户端比房主多消耗了一次随机数。

### 原因

“电子羊”原先会在每个端分别读取 `RunState.MapPointHistory`，独立构造候选牌池，然后调用 `TakeRandom(3, CombatCardGeneration)`。然而，原版游戏的部分历史记录更新并不能保证所有在线玩家持有完全相同的细节。

已经确认会造成问题的原版流程来自商店：

1. 每个端都会为所有玩家创建商店库存。
2. 玩家购买商品时，只有购买者本地库存中的对应条目会被清除。
3. `RewardObtainedMessage` 会更新远端玩家的牌组和历史记录，但不会清除其他端上该玩家商店库存中的对应条目。
4. 离开商店时，`MerchantRoom.Exit` 会扫描本地保存的所有库存，并把仍在库存中的条目记录为“跳过”。

因此，其他端可能会把某张已经购买的牌同时记录为“选择”和“跳过”，而购买者本地不会产生这条错误的“跳过”记录。这会改变“电子羊”可用历史牌池的大小，也可能改变 `TakeRandom` 对共享随机数生成器的调用次数。

### 采用的修复方案

TiebaDIY 有意不修改原版的商店或历史记录逻辑。为了尽量减少对原版及其他 Mod 的侵入，“电子羊”使用仅服务于该卡牌的 RitsuLib sidecar 同步层：

1. 卡牌拥有者根据自己的本地历史记录，生成完整且有序的可用候选牌列表。
2. 如果拥有者是客户端，则先把列表发送给房主；房主再可靠广播给所有已就绪玩家，并把广播回传给拥有者作为确认。如果拥有者就是房主，则直接广播。
3. 每个端都必须收到同一份候选列表，之后才执行原有的 `TakeRandom(3, CombatCardGeneration)`。
4. 玩家最终选择哪张牌，仍由原有的 `CardSelectCmd` 负责同步。

同步消息使用以下信息共同匹配：拥有者的网络 ID、当前共享行动 ID、来源卡牌的 `NetCombatCard` ID、出牌序号以及当前行程位置。消息会发送完整的 `SerializableCard`，以保留升级、附魔、存档属性和楼层信息。

如果消息比对应的 `OnPlay` 等待器稍早到达，它会被临时缓存。缓存会在同步超时后过期，防止迟到的数据包被后续行动错误复用。

同步失败时不会退回到“各端分别读取本地历史”的旧逻辑，因为这种静默降级会重新引入原来的随机数不同步。

### 断连与超时

“电子羊”的同步层使用原版现有的联机服务和可靠传输通道，并未建立独立连接。原版 ENet 和 Steam 的已连接超时约为 20 秒，而“电子羊”的同步等待上限为 30 秒。因此，普通断网或房主失联通常会先由原版断连逻辑处理；只有原版连接仍然存在、但附加同步消息始终未能到达时，“电子羊”才会在 30 秒后明确失败。

目前同步等待器没有直接订阅原版的断连事件。因此在极端情况下，原版已经确认某位玩家断连后，某个已经开始的“电子羊”等待仍可能保留到 30 秒超时。此设计不提供本地候选牌降级，以避免继续游戏时产生更隐蔽的状态分歧。

### 存档与重连策略

读取存档或重新加入游戏时，房主会把自己序列化的 `MapPointHistory` 快照分发给客户端。TiebaDIY 不尝试重建该快照中已经缺失的历史细节。“电子羊”直接把拥有者当前加载出的历史记录视为权威数据，无论存档中实际保存了哪些内容。

### 兼容性说明

`CardModel` 会在调用 `OnPlay` 前，把收到的 `GameActionPlayerChoiceContext` 包装成 `BranchingPlayerChoiceContext`。因此，同步层必须从 `RunManager.Instance.ActionExecutor.CurrentlyRunningAction` 获取共享行动 ID，不能直接把 `OnPlay` 收到的选择上下文强制转换为 `GameActionPlayerChoiceContext`。
